@page "/"
@inject NavigationManager NavManager


@code
{
    protected override void OnInitialized()
    {
        if (AppManager.DofusVersionIsSelected == false)
        {
            NavManager.NavigateTo("/Setup");
        }
    }
}